# git-commands-practice
this repo is to practice the git commands and to learn and understand the purposes of difference commands and how, when and why to use

git clone
git branch 
git merge
git rebase
git squash
git revert
git reset
git cherry-pick
git bisect
git add -e
git fetch --all --prune
git commit --amend
git stash
git log -- <file>
git blame <filename>
git bisect start
git bisect bad
git bisect good <older-commit-hash>
git merge --abort
git log --grep="search term"
git tag -a v1.0 -m "Version 1.0 release"
git clean -fd
git reflog
git log --graph --oneline --all
